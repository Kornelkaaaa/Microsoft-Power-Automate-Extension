{
  "name": "flow-finder",
  "version": "1.0.0",
  "description": "Manifest V3 Chrome extension: a searchable side-panel catalog of Power Automate / Power Platform connectors, actions, and triggers.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build:firstparty": "node scripts/build-firstparty.js",
    "build:catalog": "node scripts/build-catalog.js",
    "build:catalog:full": "node scripts/build-catalog.js --full",
    "build": "node scripts/build.js",
    "build:all": "node scripts/build-firstparty.js && node scripts/build-catalog.js && node scripts/build.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "keywords": [
    "power-automate",
    "power-platform",
    "connectors",
    "chrome-extension",
    "manifest-v3",
    "side-panel"
  ]
}
